import React from "react";

// import { GET_PROJECTS} from "./server/action";

const Index: React.FC<{ children: React.ReactNode }> = async () => {
  // const projects = await GET_PROJECTS();
  // if (!projects) return ;
  return (
    <>

    </>
  );
};

export default Index;

